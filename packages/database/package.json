{
  "name": "@x-llm-gateway/database",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate:pg",
    "db:migrate": "bun run src/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit",
    "build": "echo 'No build needed for TypeScript source'"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.3",
    "postgres": "^3.4.3",
    "@x-llm-gateway/shared": "workspace:*"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.9",
    "typescript": "^5.3.3"
  }
}
