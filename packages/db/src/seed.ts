/**
 * Seed placeholder — deferred to feature module migration.
 */
export async function seedSystemData(): Promise<void> {
  // Feature-specific seeding deferred:
  // - __catchall__ access model
  // - Initial route rules
}
